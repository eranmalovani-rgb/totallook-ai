Login Loop Debug Notes:

1. auth.me returns user data successfully (status 200, user id 60066, Eran Malovani)
2. profile.get returns null (profile was deleted when account was deleted)
3. After deleteAccount, user re-created via OAuth but profile is null
4. Home.tsx checks: if profile && !profile.onboardingCompleted → redirect to /onboarding
5. But profile is null (not just onboardingCompleted=false), so the condition doesn't match
6. The else if checks: profile && profile.onboardingCompleted → redirect to /upload
7. Neither condition matches when profile is null → user stays on Home page
8. BUT the user said they keep getting redirected to login screen...

Wait - the user said they deleted their account. So:
- deleteAccount deleted the user from DB
- User clicks login again → OAuth creates new user via upsert
- auth.me returns the new user (works)
- profile.get returns null (no profile exists for new user)
- Home.tsx: isAuthenticated=true, profile=null
- useEffect: authLoading=false, profileLoading=false, isAuthenticated=true
- profile is null, so neither redirect fires
- User sees the landing page with "התחבר והתחל" button even though they're logged in

Actually re-reading: the issue might be that after account deletion, the user 
is re-created but the Home page shows the landing page (for non-auth) but user IS authenticated.
The landing page has "התחבר והתחל" which goes to login, creating a loop.

FIX: When profile is null but user is authenticated → redirect to onboarding (new user flow)
