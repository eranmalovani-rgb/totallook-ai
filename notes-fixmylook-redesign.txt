Fix My Look Alternatives Panel - Current State (from screenshot):

1. Each alternative card shows:
   - Title (e.g., "שדרוג הסווטשירט למותג יוקרה")
   - Description text (long paragraph)
   - Before → After labels (e.g., "סווטשירט New York Knicks" → "סווטשירט מעצבים יוקרתי")
   - Shopping links with small product thumbnails (Farfetch, SSENSE, Harrods)
   - Radio button for selection

2. User's request:
   - REMOVE shopping links from alternatives panel
   - SHOW product images INLINE (large, visual)
   - User selects alternative BY THE IMAGE
   - AI generates visualization based on the selected image/alternative
   - No need to redirect to stores

3. Design plan:
   - Replace shopping links section with a grid of product images (from shoppingLinks[].imageUrl)
   - Make images larger and clickable
   - Keep the title, before→after labels
   - Shorten or remove the long description
   - Make the whole card more visual/image-focused
   - Keep radio button selection
